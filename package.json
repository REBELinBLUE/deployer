{
  "private": true,
  "name": "deployer",
  "dependencies": {
    "dotenv": "^2.0.0",
    "ioredis": "^2.2.0",
    "socket.io": "^1.4.5",
    "jsonwebtoken": "^7.0.0"
  },
  "scripts": {
    "postinstall": "touch ./node_modules/.install",
    "clean-build": "rm -rf public/build/",
    "clean": "npm run clean-build; npm run clean-deps",
    "clean-deps": "rm -rf vendor/ node_modules/ resources/assets/node_modules",
    "install-deps": "composer install --no-suggest; npm install; cd resources/assets; npm install; cd ../.."
  }
}
