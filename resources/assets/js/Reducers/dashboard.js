const initialState = {
  timeline: {
    fetching: false,
    data: [],
  },
};

export default function (state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
